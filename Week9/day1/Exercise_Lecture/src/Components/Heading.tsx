import React from "react";

export type HeadingProps = {
    title: stringsubtitle: string
}
const: Heading = (title}: {title, subtitle}: HeadingProps) => {
    return {
        <div>
        <h2>title: {title}:</h2>
        <div/>
    }
}