/**
 * ─────────────────────────────────────────────────────────────
 * PURPOSE   Router and route guards.
 * WHY       Central place to enforce that unauthenticated users cannot reach project pages.
 * DEPENDS   pages/*, context/AuthContext
 * ─────────────────────────────────────────────────────────────
 *
 * TODO
 *   [ ] Routes: /login, /projects, /projects/:id/phase/:n, /projects/:id/documents
 *   [ ] ProtectedRoute wrapper reading AuthContext
 */

// TODO
export function App() { return null; }
