/**
 * ─────────────────────────────────────────────────────────────
 * PURPOSE   Renders one question by its type: text | select | multiselect | number.
 * WHY       One component switching on type keeps the phase page simple and means adding a question type is a single-file change.
 * DEPENDS   lib/types.ts
 * ─────────────────────────────────────────────────────────────
 *
 * TODO
 *   [ ] Enforce numeric min/max from the bank
 *   [ ] Show helpText beneath the label
 *   [ ] Mark optional questions visibly
 */

// TODO
